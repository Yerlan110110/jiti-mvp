services:
  - type: web
    name: jiti-backend
    env: node
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20.11.0"
      - key: DATABASE_URL
        value: "jiti.db"
      - key: CORS_ORIGINS
        value: "*"
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_PHONE
        value: "+77001234567"
      - key: SMS_MOCK
        value: "true"
      - key: SMS_MOCK_CODE
        value: "1234"
